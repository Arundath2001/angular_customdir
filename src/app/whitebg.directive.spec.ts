import { WhitebgDirective } from './whitebg.directive';

describe('WhitebgDirective', () => {
  it('should create an instance', () => {
    const directive = new WhitebgDirective();
    expect(directive).toBeTruthy();
  });
});
