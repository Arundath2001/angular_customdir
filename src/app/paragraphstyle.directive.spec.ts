import { ParagraphstyleDirective } from './paragraphstyle.directive';

describe('ParagraphstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ParagraphstyleDirective();
    expect(directive).toBeTruthy();
  });
});
