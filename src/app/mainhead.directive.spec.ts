import { MainheadDirective } from './mainhead.directive';

describe('MainheadDirective', () => {
  it('should create an instance', () => {
    const directive = new MainheadDirective();
    expect(directive).toBeTruthy();
  });
});
