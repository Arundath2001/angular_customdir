import { HeadstyleDirective } from './headstyle.directive';

describe('HeadstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadstyleDirective();
    expect(directive).toBeTruthy();
  });
});
