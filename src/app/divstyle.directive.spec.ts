import { DivstyleDirective } from './divstyle.directive';

describe('DivstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new DivstyleDirective();
    expect(directive).toBeTruthy();
  });
});
