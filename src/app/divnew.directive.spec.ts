import { DivnewDirective } from './divnew.directive';

describe('DivnewDirective', () => {
  it('should create an instance', () => {
    const directive = new DivnewDirective();
    expect(directive).toBeTruthy();
  });
});
