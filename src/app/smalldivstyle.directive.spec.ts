import { SmalldivstyleDirective } from './smalldivstyle.directive';

describe('SmalldivstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new SmalldivstyleDirective();
    expect(directive).toBeTruthy();
  });
});
